**NO-GO as written.** Fix the verdict definition, incomplete-data policy, and reporting/budget failures before starting N2. On complete data, the primary estimand, equal SH graph weighting, nested bootstrap, seed, and resample count match §4.

1. **MAJOR — The report command crashes.** The anchor tripwire returns a NumPy boolean at [analysis.py:339](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:339); the CLI’s ordinary `json.dumps` cannot serialize it. I reproduced the failure on synthetic records. The smoke test conceals it with `default=float` at [test_exp02_report.py:71](/D:/Claude/random/wormWars/tests/test_exp02_report.py:71). Convert to native `bool` and test the actual CLI serialization. **Original point 9 is not fully fixed.**

2. **MAJOR — The verdict mixes two different hypotheses.** With N2 use CI `[0.20, 0.30]`, Δ CI `[0.001, 0.009]` is **supported**, but `[-0.001, 0.009]` is **challenged**. Both establish an advantage smaller than 0.10; only one excludes zero. The first two challenged branches reasonably contradict the registered conjunction. The equivalence branch instead challenges a *meaningful between-group advantage*, which support never requires. This is faithfully implemented at [analysis.py:220](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:220), so the problem is the rule itself. Either remove that branch, or register a separate Δ margin and apply it consistently. The 0.10 absolute-use threshold is a defensible screening convention; its scripted-gain rationale does not automatically justify a Δ equivalence margin.

3. **MAJOR — Incomplete primary data can silently produce “supported.”** Missing probes are skipped at [analysis.py:205](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:205), without checking expected units, 64 scores, IDs, seeds, or finiteness. I reduced synthetic input to **one N2 run and one SH graph/run**: the primary returned “supported” with zero-width intervals. Freeze minimum replication and permissible schedule prefixes; require complete primary probes for every retained unit. Otherwise withhold the verdict. This closes a consequential exclusion/stopping path.

4. **MAJOR — The registered budget fallback is not implemented.** [PREREGISTRATION.md:177](/D:/Claude/random/wormWars/experiments/02-screening/PREREGISTRATION.md:177) promises dropping behaviour, then integrator rescoring. [scripts/exp02.py:446](/D:/Claude/random/wormWars/scripts/exp02.py:446) always runs both for g39; `cmd_probes` has no budget controller and saves only at the end. Removing integrator results causes a reproduced `KeyError` at [analysis.py:250](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:250). Specify and implement when omissions occur, how consumed time is counted, and how omitted tripwires become “not assessed.”

5. **MAJOR — The SH prevalence statement is unjustified.** “Zero of eight bounds the rate below about 31%” at [PREREGISTRATION.md:100](/D:/Claude/random/wormWars/experiments/02-screening/PREREGISTRATION.md:100) assumes reliably observed binary outcomes. Here “meaningful” requires an interval to clear 0.10; genuinely capable graphs can remain inconclusive. The bound could describe the probability of a **detected result under this procedure**, not capability prevalence. Moreover, [report.py:92](/D:/Claude/random/wormWars/wormwars/exp02/report.py:92) averages separate CI endpoints; that is not a bootstrap interval for the graph’s mean. Define the graph classification properly and narrow or remove the prevalence claim.

6. **MINOR — Convergence grouping is fixed, but the registered quantity is wrong.** [analysis.py:164](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:164) computes `c·log(10)`: time to complete 90% of the fitted **improvement**, not reach 90% of the asymptotic score. For `y(g)=3−0.1 exp(−g/100)`, the initial score already exceeds 90% of the asymptote, yet the function reports late convergence. I reproduced this. Rename the criterion or correct the equation before freezing. **Original point 11 is only partly resolved.**

7. **MINOR — An explicitly permitted budget cut breaks variance estimates.** [analysis.py:121](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:121) assumes equal runs per graph using the first graph’s count. Cutting halfway through SH second runs gives mixed counts of one and two; I reproduced NaN between- and within-graph components. Handle unequal replication or report these components unavailable. This does not change primary SH weighting.

8. **MINOR — Several secondary specifications do not match execution.** Behaviour uses **four**, not 16, checkpoint worlds ([scripts/exp02.py:452](/D:/Claude/random/wormWars/scripts/exp02.py:452)). Valence checks cover N2 and SH1 under M0, despite §5’s “per graph and mapping” wording. Integrator interactions use **raw scores** ([analysis.py:250](/D:/Claude/random/wormWars/wormwars/exp02/analysis.py:250)), whereas §5 defines fitness interactions on normalized scores. State the actual scope and units, especially for the 0.02 tripwire threshold.

9. **MINOR — “All 11 adopted” is not presently auditable.** The original numbered review is absent from the files I found; D038 and the ledger summarize it. Source annotations let me verify points **3, 4, 5 and 10**: corrected common-mode comparison, equivalence validation, mean/swap implementation, and saved per-world probes are implemented. Points **9 and 11** have the defects above. Also, the exploration README discloses N2’s **0.16 common-mode response** alongside 0.12, so “one N2 number” should be corrected. Archive the original review and its disposition before committing the registration.

Verification: **22 analysis/report tests passed**, despite the reproduced failures above. All five registered hashes match the committed blobs; calibration’s maximum validation error is **3.762%**. Mean/swap correctly operate before scaling and clipping. I changed no files and ran no N2 evolution or scoring.